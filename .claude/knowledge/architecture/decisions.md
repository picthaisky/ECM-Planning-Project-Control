# Architecture Decision Records (ADR)

Append-only log. Format per entry:

```
## ADR-NNNN: <title>
Date: YYYY-MM-DD · Status: Accepted | Superseded by ADR-XXXX
Context: <the problem/forces>
Decision: <what we chose>
Consequences: <what this binds us to>
```

Agents must not contradict an Accepted ADR; to change one, write a new ADR that supersedes it.

---

## ADR-0001: Clean Architecture + DDD with CQRS/MediatR on .NET 10
Date: 2026-07-11 · Status: Accepted
Context: docs/3. mandates strict layering for an 8-month, 4-phase build with parallel AI agents; parsers/engines must stay testable and swappable.
Decision: Four projects (Domain / Application / Infrastructure / WebApi); CQRS via MediatR; FluentValidation in Application; EF Core 10 + MSSQL in Infrastructure only. CPM/EVM engines are pure Application services.
Consequences: More boilerplate per feature, but agents can work in parallel without layer conflicts; engines unit-test without DB.

## ADR-0002: Multi-tenant isolation via TenantId + EF global query filters
Date: 2026-07-11 · Status: Accepted
Context: docs/4. specifies multi-tenant SaaS; cross-tenant leakage of contract/payment data is the worst-case security failure.
Decision: Every tenant-owned table carries `TenantId` (indexed, leading column of composite indexes); a global EF query filter bound to `ITenantProvider` applies on all reads; writes stamp TenantId server-side, never from client input.
Consequences: Single database, simple ops; security-auditor treats any unfiltered query as release-blocking; admin cross-tenant tooling needs explicit filter bypass with audit.

## ADR-0003: MS Project integration via MSPDI XML (MPXJ), never binary .MPP or interop
Date: 2026-07-11 · Status: Accepted
Context: docs/วิเคราะห์ฯ §2 — binary .MPP parsing/interop is unstable and slow in cloud containers.
Decision: Import/export MS Project data through MSPDI XML using MPXJ.Net; XER parsed natively (tab-delimited %T tables); Excel via EPPlus.
Consequences: Users export .mpp → MSPDI when needed; XML hardening (XXE, size caps) is mandatory on the import path.

## ADR-0004: Virtualized rendering for Gantt and large tables
Date: 2026-07-11 · Status: Accepted
Context: docs/2. risk #1 — browser performance with 10,000+ activities is a High risk.
Decision: react-window-style row virtualization for all large lists and the Gantt; bars drawn on a canvas/single-SVG layer; heavy series computation in Web Workers.
Consequences: Gantt components must keep row props stable/memoized; no DOM-per-bar designs will pass review.

## ADR-0005: Offline-first site modules via IndexedDB outbox + Background Sync
Date: 2026-07-11 · Status: Accepted
Context: docs/1. §5 and docs/2. risk #3 — site connectivity is unreliable; photo/weather/progress capture must not block on network.
Decision: Site-facing writes enqueue into an IndexedDB outbox (client-compressed photos included) and flush via Service Worker Background Sync; server wins on schedule data, last-write-wins + audit on site logs.
Consequences: Every site-module feature ships with offline tests; sync status must be visible in the UI.

## ADR-0006: Adopt the working prototype's navy/gold theme as the design system source of truth
Date: 2026-07-12 · Status: Accepted
Context: `docs/3.` describes a "warm orange-brown" palette (`#E26D5C` primary, Inter font) conceptually, but the user has since supplied a working interactive prototype (`docs/ECM Planning Prototype.dc.html` + Standalone export) covering all 13 screens with a concrete, fully worked-out navy/gold visual language (`#0F2542` navy, `#C9A227` gold, IBM Plex Sans Thai + Bai Jamjuree fonts). The prototype is more concrete and functionally complete than the earlier text description and reflects the actual product direction.
Decision: The prototype HTML files are now the authoritative visual/UX reference. `docs/3.`'s orange-brown narrative is superseded for styling purposes (its architecture content elsewhere remains valid). All frontend work reads `/cmplus-ui` (updated with the real tokens) and the prototype file directly before implementing any screen.
Consequences: `frontend-developer` and `system-architect` must not reintroduce the old orange palette. Any new screen must first be checked against the prototype's 13-screen nav; screens implied by `docs/6.`'s 15-module list but absent from the prototype (standalone "Executive Summary", standalone "Daily/Weekly Progress") need explicit product confirmation from `po-analyst`/human before being added or dropped — see `.claude/knowledge/domain/modules-map.md`.

## ADR-0007: EAC is a selectable variant computed by one Performance-Factor engine — five variants in the engine, three in the v1 UI
Date: 2026-07-27 · Status: Accepted
Context: Two upstream artifacts written in parallel diverged. `docs/specs/master-plan/backlog-detailed.md` (§1, US-7.2) scoped a 3-value enum `Project.DefaultEacVariant` (`BacOverCpi`/`Atypical`/`Combined`) with a dropdown showing exactly those three. `docs/specs/master-plan/domain-decisions.md` §1 formalised **five** variants (`CpiBased`/`Atypical`/`CpiSpiBased`/`BottomUpEtc`/`CustomPf`) as one unified engine $ETC = PF\times(BAC-EV)$, recommended `Project.EacVariantDefault` + `EacCustomPerformanceFactor` + `EacManualEtc`, and recommended returning every computable variant per response. The engine cost of all five is negligible ("five multiplications on four scalars") and the edge-case matrix, invariants and fixtures (A1–A5, D1–D3, E–I) are already written. The real cost of the two extra variants is **UI**: `BottomUpEtc` and `CustomPf` each need a persisted project input, validation, permissioning and user guidance.
Decision: (a) The **engine implements all five variants** from Sprint 7 as a single pure Application service using the unified PF form; there is no per-formula code branch. (b) `GET /api/v1/projects/{id}/evm` returns **every computable variant** with an explicit `null` + machine-readable reason (`NotStarted`/`NoActualCost`/`NoPlannedValue`/`ZeroCpi`) for the rest, plus `selectedVariant`. (c) Field naming follows domain-expert: `Project.EacVariantDefault` (enum, NOT NULL, default `CpiBased`), `Project.EacCustomPerformanceFactor decimal(9,4) NULL` (> 0), `Project.EacManualEtc decimal(18,2) NULL` (≥ 0). po-analyst's `Project.DefaultEacVariant` and the values `BacOverCpi`/`Combined` are **superseded**; the mapping is `BacOverCpi` → `CpiBased`, `Combined` → `CpiSpiBased`. (d) The **v1 UI (Sprint 7) surfaces only the three index-based variants** in the selector; `BottomUpEtc`/`CustomPf` are computed and returned but their input UI lands in Sprint 14. (e) EAC is never persisted on `Project` — only inside immutable `EvmPeriodSnapshot` rows together with the variant and the PF actually used. (f) Changing `EacVariantDefault` is a mutating domain operation (audit log) restricted to PM/QS/Executive — this closes backlog open question D4 as an architecture decision, not a domain one.
Consequences: One engine, one test suite; adding a variant to the UI later is a frontend-only change. QA must cover all five variants and the edge matrix from Sprint 7, not just the three shown. The frontend must never assume a fixed EAC ordering when colour-coding (ordering reverses when CPI > 1). Any golden-file EAC comparison against P6 is invalid until the source project's actual P6 Earned Value setting is confirmed (risk R-01 in `docs/10.`).

## ADR-0008: Approval authority is an amount-tiered, version-pinned policy engine built from Sprint 2 (not a role-only boolean matrix)
Date: 2026-07-27 · Status: Accepted
Context: Second parallel divergence. `backlog-detailed.md` scoped `ApprovalMatrixEntry(TenantId, Module, Role, CanApprove)` — a per-role boolean — landing Sprint 2 (model) / Sprint 9 (admin UI) / Sprint 10 (VO reuse). `domain-decisions.md` §3 and `.claude/knowledge/domain/approval-workflow.md` §5 argue for an amount-tiered `ApprovalPolicy` / `ApprovalPolicyRule` / `ApprovalAction` model, because a role-only matrix cannot express the single most common real DoA rule ("PM approves VOs under ฿500,000; above that the Project Director must also sign") and would force per-tenant code branches. The choice is build-simple-then-migrate versus build-rich-now. Build-then-migrate is not a cheap path here: the boolean check is a *different call shape* from a multi-step chain (chain snapshot, `StepNo` pointer, `RevisionNo`, quorum, self-approval rules), so the enforcement hook, the write API, the admin UI and their tests would all be discarded — and the data to migrate is approval history, i.e. legal evidence, which must never be rewritten in place. Meanwhile the rich model has **no remaining design work**: the routing algorithm (§5.3), the state machines (§4) and fixtures R1–R10 are fully specified.
Decision: Build the amount-tiered model directly, starting Sprint 2. `ApprovalMatrixEntry` is superseded and must not be created. A role-only DoA is expressed as a single-step rule spanning `[0.00, NULL)` — the simple case is a *configuration* of the rich model, never a second code path. `User.Role` gains `ProjectDirector` (required for a 3-tier chain; adding it at Sprint 2 is free, later it is a claims/enum migration). Surface is staged to keep Sprint 2 bounded: **Sprint 2** = the three tables + seeded `TH-Default-VO`/`TH-Default-IPC` policies + a pure `IApprovalRoutingService` (fail-closed, version-pinned) + read-only `GET /api/v1/tenants/{id}/approval-policies`; **Sprint 9** = Payment Certificate state machine, policy write API, Tenant Admin policy editor UI; **Sprint 10** = VO chain + cumulative-VO escalation; **Sprint 15** = policy version history + routing simulator. Schema-present but deliberately not surfaced in v1: `RequiredUserId`, `QuorumCount > 1`, project-scoped policy override (`ApprovalPolicy.ProjectId`), delegation (out of scope entirely).
Consequences: Sprint 2 grows by one table-set plus a routing service; Sprints 9 and 10 shrink correspondingly and carry no rework. `ApprovalAction` is append-only legal evidence — corrections are compensating rows, never edits or migrations. Every approve/reject endpoint resolves the chain server-side, tenant-scoped (ADR-0002); an empty chain fails closed with `ApprovalPolicyGap` (422) and must never auto-approve. Policy edits create `Version+1`; in-flight documents keep the version that routed them. `security-auditor` must review the policy endpoints and routing before Sprint 9 close and before Sprint 10 close — not only at Sprint 15.

## ADR-0009: `ActivityProgressLog` is the system of record for progress over time; historical EVM reads the latest dated snapshot as of $t$; closed periods are frozen in `EvmPeriodSnapshot`
Date: 2026-07-27 · Status: Accepted
Context: `docs/9.` §4 stores only `Activity.ProgressPercentage`, overwritten in place, so historical PV/EV could only be reconstructed by parsing `AuditLog.BeforeJson/AfterJson` — fragile and unfit for a reporting engine. `backlog-detailed.md` §2.2 introduced an append-only `ActivityProgressLog` and raised open question D1 (what the historical reconstruction rule should be). `domain-decisions.md` was produced in parallel and never saw the new entity, so D1 was never answered. This is a data-modelling decision, not a formula question.
Decision: `ActivityProgressLog` (append-only, immutable, same pattern as Weather Log) is authoritative for progress over time. Reconstruction is a **step function, no interpolation**: $Pct_i(t)$ = the `ProgressPercentage` of the entry for activity $i$ with the greatest `PeriodEndDate` ≤ $t$ (ties broken by greatest `RecordedAt`); an activity with no entry ≤ $t$ contributes 0. $EV(t) = \sum_i BudgetCost_i \times Pct_i(t)/100$. `Activity.ProgressPercentage` is a **denormalised cache** of the entry with the greatest `PeriodEndDate`; a backdated correction is appended and does **not** move the cache. Every write path (batch grid, Excel import, offline sync, photo-linked update) writes a log row; no path writes the cache directly. Derived series legitimately change when a backdated entry lands — that is correct and fully audited because the log is append-only — so **closed reporting periods are frozen** in an immutable `EvmPeriodSnapshot` (data date, BAC, PV, EV, AC, EAC variant + PF used) which is what dashboards, exported PDFs and payment certificates cite for past periods. Closes backlog open question D1.
Consequences: mandatory index `(TenantId, ActivityId, PeriodEndDate DESC)` plus `(TenantId, PeriodEndDate)` for period rollups; `database-engineer` must watch row volume (10,000 activities × weekly for 8 months ≈ 350k rows/project — see risk R-11). QA owns an out-of-order backfill fixture from Sprint 1. Reports that must be reproducible cite a snapshot id, never a live recomputation.

## ADR-0010: Cloud-agnostic containerization with the AWS-vs-Azure decision deferred to an explicit gate inside Sprint 16
Date: 2026-07-27 · Status: Accepted
Context: `docs/9.` §11 item 5 (AWS ECS vs Azure Container Apps) was deliberately deferred by the human. Waiting for the answer would stall all infrastructure work until Phase 4; guessing risks rewriting the deployment path and any provider SDK usage that leaks into application code.
Decision: Make provider choice a late, cheap decision. (a) **From Phase 0**: multi-stage Dockerfiles (API and web), a `docker-compose` stack (api + web + MSSQL + object storage) that is the same topology as production, configuration exclusively via environment variables, and `/health/live` + `/health/ready` endpoints. (b) **From Sprint 1**: generic CI — build → test → lint → security scan (vulnerable NuGet/npm packages, image scan) → push an OCI image tagged by commit SHA to an OCI-compatible registry (GHCR by default). (c) **No provider SDK in application code**: object storage sits behind `IFileStorage` in Infrastructure with a local/S3-API-compatible adapter for dev and staging; the provider adapter is written only after the gate. Secrets come from `IConfiguration`/env only. EF migrations are emitted as an idempotent SQL script artifact applied by a separate, human-approved job. (d) **Sprint 16 is split**: *16A* a provider-agnostic staging deployment (compose or single-node k3s) that satisfies US-16.1's human sign-off gate; *16B* an explicit **decision gate** — the human picks AWS ECS Fargate or Azure Container Apps — followed by execution of the matching ready-written runbook. If the gate is not passed by the end of Sprint 15, 16A still completes and production deployment becomes a separately scheduled runbook execution; the 16-sprint timeline is not blocked.
Consequences: No provider-specific managed service (queues, functions, provider-native identity) may be introduced before the gate without a superseding ADR. The only provider-specific code is the storage/secrets/telemetry adapter set, isolated in Infrastructure. Staging must stay functionally equivalent to the production topology so the gate changes a deployment target, not an architecture. Both runbooks live in `docs/10.` §13 and must be kept executable.

## ADR-0011: MSPDI parsing ships an interim hand-written reader behind `IMspdiScheduleParser`; MPXJ.Net is not currently buildable on this toolchain
Date: 2026-07-29 · Status: Accepted (interim; revisit when the upstream defect is fixed)
Context: ADR-0003 specifies MPXJ.Net for MSPDI parsing. Adding `PackageReference Include="MPXJ.Net"` (tried: 13.0.0, 16.5.0 — the oldest and newest available) fails `dotnet build` unconditionally, before any application code compiles. Root cause independently reproduced twice — once by `backend-developer` inside the real solution, once by the orchestrator in an isolated scratch console project with no other dependencies — narrowing it to MPXJ.Net's own transitive dependency chain, not this repo's configuration: `IKVM.Util.Jar.JarFileUtil.GetModuleNameAndVersionFromFileName` throws `ArgumentOutOfRangeException: count ('1') must be less than or equal to '0'` while IKVM.Maven.Sdk transpiles MPXJ's Java dependency graph, specifically on `commons-codec-1.15.jar`. That jar was checked directly (`file` + magic-byte inspection: a valid, uncorrupted JAR/ZIP, 353,793 bytes, standard Maven Central artifact) — the defect is in IKVM's own filename-parsing logic for this jar's name shape, not a bad download. Reproduced identically across IKVM 8.7.5, 8.14.0, and 8.15.0 (three versions spanning the full currently-published range) paired with both tried MPXJ.Net versions — six failing combinations, zero succeeding. Network access to both Maven Central and NuGet was confirmed working, ruling out a connectivity/proxy explanation. Because `commons-codec` is a foundational, rarely-bumped transitive dependency reachable from MPXJ.Net's Java side (via Apache POI) regardless of which MPXJ.Net version is selected, and NuGet/Maven dependency resolution is deterministic, this is expected to reproduce identically in CI/Docker or any other build environment — it is a genuine, currently-unpatched upstream defect in the IKVM/MPXJ.Net toolchain combination, not an artifact of this sandbox.
Decision: Ship Sprint 3 with a hand-written `MspdiScheduleParser` (`backend/src/CMPlus.Infrastructure/Parsers/Mspdi/`) reading the MSPDI XML schema directly, hardened against XXE the same way any untrusted XML input in this codebase must be (`DtdProcessing.Prohibit`, `XmlResolver = null`). It sits behind `IMspdiScheduleParser` (`backend/src/CMPlus.Application/Abstractions/`), the exact seam MPXJ.Net would occupy, so adopting the real library later — once IKVM fixes this upstream, or MPXJ.Net bumps past the affected commons-codec version — is a single-file swap with zero ripple into Application/WebApi/tests. ADR-0003's actual intent (never binary `.mpp`/COM interop) is fully preserved; only the specific library named in that ADR is deferred. This ADR does not reopen or contradict ADR-0003 — it records why its named implementation isn't in place yet and what closes the gap.
Consequences: The interim parser has no upstream Java-side test corpus behind it — its golden-file coverage is only as good as the hand-built, spec-compliant fixtures `backend-developer`/`qa-engineer` authored this sprint (see the fixture-provenance note in `backend/tests/fixtures/goldenfiles/`), not verified against real MS Project output. `devops-engineer` should re-attempt `MPXJ.Net` in the actual CI/Docker build image opportunistically (e.g. when bumping any related package) to check whether an upstream fix landed, but should not expect a different outcome merely from a different machine. Any future attempt must re-run this same isolated-repro check before assuming the fix is real. No other parser (XER, Excel) is affected — this is specific to the Java-interop path only.

## ADR-0012: A relation graph rejects a duplicate `(predecessor, successor)` pair outright — it is never deduplicated or silently merged, regardless of `RelationType`
Date: 2026-07-29 · Status: Accepted
Context: Neither `cpm-method.md` nor `cpm-fixtures.json` specifies what should happen when a relation graph (from XER/MSPDI import, Sprint 3, or the CPM engine's own graph validation, Sprint 5) contains two relations between the same activity pair — e.g. both an FS and an SS from A to B. This was flagged as an open gap by `backend-developer` during Sprint 3's parser work and again, independently, by `backend-developer` and `qa-engineer` during Sprint 5's CPM engine work — the same question surfacing twice from two different codepaths (`RelationGraphValidator` in Infrastructure for imports, `GraphValidator` in Application for the CPM engine, which cannot share code across the Application/Infrastructure boundary per ADR-0001) is a sign it needed a real decision, not another flag. `qa-engineer`'s Sprint 5 cross-validation confirmed both validators already independently converged on the same behavior without coordinating, which is itself evidence this is the natural, low-risk default.
Decision: A duplicate `(PredecessorActivityId, SuccessorActivityId)` pair — keyed on the activity pair alone, **not** including `RelationType` (so an FS and an SS between the same two activities is still a duplicate, not two independent constraints) — is rejected as malformed input. This applies uniformly at every graph-construction boundary: XER/MSPDI import (`RelationGraphValidator`) and the CPM engine's own graph validation (`GraphValidator`). There is no dedupe/merge/"most restrictive wins" behavior anywhere. Consistent with this project's established fail-closed philosophy (ADR-0008's approval routing, ADR-0002's tenant isolation): an ambiguous or conflicting source schedule is a data-quality problem to surface to the user, not a decision for CM+ to make silently on their behalf by picking one relation to honor.
Consequences: A `.XER`/MSPDI file or a UI-created relation set with a genuine duplicate pair fails validation with the specific offending pair identified, before any CPM calculation runs — never a silently-dropped or silently-merged constraint. If a future real-world P6/MSP export is found to legitimately contain such duplicates (some scheduling tools do allow redundant relations as a modeling convenience), this ADR must be revisited with that concrete evidence rather than assumed away.

## ADR-0013: Actual Cost is an append-only `ActualCostEntry` ledger on an accrual basis, contractor-side, never derived from certified payments
Date: 2026-08-09 · Status: Accepted
Context: Sprint 7 built the EVM engine and the 5-variant EAC calculator, and implementation surfaced that `AC` (ACWP) — one of the three fundamental EVM inputs — **had no home in the data model at all**. `evm-formulas.md` defined it as "$\sum$ actual cost recorded $\le t$", but nothing recorded it: verified independently against `docs/9.` §4, `docs/10.` §3's data-model delta table, and `backlog-detailed.md`. `backend-developer` correctly refused to invent a field or fabricate a number, shipping an honest `IActualCostReader` placeholder returning `0` that routes every real project into the `NoActualCost`/`ZeroCpi` null-reason paths rather than producing plausible-looking wrong money. Consequence while unresolved: `CV = EV − AC`, `CPI = EV / AC` and every EAC variant are unusable in production, and Sprint 8 (Cash Flow, Executive Dashboard) consumes exactly those figures. `domain-expert`'s full ruling is `.claude/knowledge/domain/actual-cost.md`; this ADR records the decisions and freezes the schema.
Decision: (a) **AC is cost incurred on an accrual basis** (PMBOK/EIA-748/AACE), matched to the same work EV measures — not committed cost (which spikes on PO signature, before any work), not paid cost (which flatters CPI by one credit cycle). Month-end accruals are mandatory and are reversed against the invoice. (b) **Certified payments are never AC.** Confirmed and strengthened from `domain-decisions.md` §3.1: certified value is EV *marked up to contract price*, so substituting it makes CPI measure margin rather than cost. Fixture AC-7 makes the danger concrete — the same project reads CPI 1.0504 / CV +฿120k on cash received versus 0.7716 / −฿740k on truth: **the sign of CV flips**. `ProjectFinanceLedger` (retention/advance on certified payments) and `ActualCostEntry` are two deliberately separate ledgers and must never be conflated. (c) **The CM+ tenant is contractor-side** — confirmed by the human 2026-08-09, closing the ruling's highest-impact open question (Q1). AC is therefore money the contractor *spends*. The ledger shape is perspective-neutral, so supporting an employer/PMC tenant later is a `Project.Perspective` flag, not a migration. (d) **Entity: `ActualCostEntry`**, append-only with no UPDATE/DELETE path, per `actual-cost.md` §9's full field list — deliberately *not* named `…Ledger` to keep it unconfusable with `ProjectFinanceLedger`. Bitemporal: `IncurredDate` is valid time and drives $AC(t)$; `PostedAt` is transaction time and is audit-only. Granularity is control-account (`WbsNodeId`, nullable) with optional `ActivityId` and a Thai 5-หมวด `CostCategory`; node/activity CPI is reported only where AC is genuinely attributed there — a parent's AC is never pro-rated down to children. (e) **Backdating is allowed and restates live figures**, consistent with ADR-0009's treatment of `ActivityProgressLog`; closed `EvmPeriodSnapshot` rows stay frozen, and the divergence is surfaced as an explicit `periodRestated` delta rather than silently absorbed. (f) `IActualCostReader` returns `ActualCostResult(decimal Amount, int EntryCount)` so "no cost recorded yet" and "costs recorded, netting to zero" are distinguishable rather than both reading as a bare `0`.
Consequences: **Sprint 8 is not viable until this lands** — its Cash Flow AC series, its CV/CPI/EAC/ETC/VAC dashboard tiles, and its prototype-match acceptance criterion all depend on real AC; the human's 2026-08-09 sequencing decision is therefore to build `ActualCostEntry` first and then proceed into Sprint 8 (trading S8-BE-03's PDF export to Sprint 9 if capacity binds). AC entry needs both a QS manual-entry UI (accruals never come from an ERP) and an Excel/CSV import of the monthly job-cost ledger; a `CostCodeMapping` table supports the import path. Overhead allocation is **excluded** — CM+'s EVM is explicitly site-cost basis, since including it without `BAC` also carrying it would permanently depress CPI. THB only in v1. One known latent defect to fix while implementing: `EvmPeriodSnapshot`'s constructor calls `MoneyGuard.EnsureNonNegative(Ac)`, which **throws** — unreachable while AC is pinned at 0, but a credit note or over-reversal producing negative AC would make closing a period throw instead of degrading gracefully (found by `qa-engineer` during Sprint 7 review). `actual-cost.md` §13 keeps five open questions; Q3 (does the pilot contractor have an exportable job-cost ledger, and in what format) must be answered with the client's accounting team before the import path is designed.

## ADR-0014: Executive Summary PDF is rendered by headless Chromium (Playwright), not QuestPDF — chosen on licensing grounds
Date: 2026-08-09 · Status: Accepted
Context: `docs/10.` §7 Sprint 8 explicitly required confirming the PDF library's **license before any code was written**, naming QuestPDF as the primary candidate. Verified against QuestPDF's own license guide: the free Community tier requires annual gross revenue under **USD 1,000,000** *and* explicitly excludes **public-sector/government agencies and publicly traded companies**. Both exclusions land squarely on CM+'s actual customer base — this product's own domain knowledge already models Thai government procurement (พ.ร.บ.การจัดซื้อจัดจ้างฯ 2560, see `payment-retention.md`/`approval-workflow.md`), and large Thai contractors are commonly SET-listed. Community would therefore be unusable for a realistic share of customers, making a recurring paid license the real cost of that path. This repo already carries one unresolved licensing debt of exactly this shape — EPPlus runs on a Polyform **Noncommercial** license and still needs a commercial key before production (Sprint 3 finding L-06) — so adding a second was worth avoiding deliberately. Secondary consideration: Thai script needs correct complex-script shaping (vowel/tone-mark positioning), which is a genuine risk with general-purpose PDF libraries and a non-issue for a real browser engine.
Decision: Render the Executive Summary PDF with **headless Chromium driven by Playwright** (Apache-2.0; Chromium BSD) — confirmed by the human 2026-08-09. Rationale beyond licensing: Playwright is already a repo dependency (added Sprint 6 for the Gantt E2E suite), so this introduces no new licensed component; Chromium renders Thai correctly *by construction*, because it is the same engine already rendering the app's own Thai UI; and the report can reuse the real React components and CSS tokens rather than re-implementing the S-Curve and KPI tiles a second time server-side, which removes a whole class of "the PDF drifted from the screen" bugs. QuestPDF is rejected for this project, not judged inferior — a different product with a sub-$1M private customer base could reasonably choose it.
Consequences: **The API container must not simply absorb Chromium** — Phase 0 set a 300 MB API image budget (ADR-0010) and a browser exceeds it on its own. The renderer belongs in its own container/service invoked by the API, keeping the API image lean and the topology still provider-agnostic; `devops-engineer` owns making that concrete, and the image-size implication must be measured, not assumed. Rendering is now an I/O call to a separate process, so the export path needs a real timeout and a clear failure state surfaced to the user (S8-FE-03's DoD already requires showing generating/failed states). The Thai-font DoD ("ฟอนต์ไทยแสดงถูกต้อง — ไม่กลายเป็นกล่องว่าง") still requires the renderer image to actually ship the IBM Plex Sans Thai / Bai Jamjuree fonts; a browser that lacks them renders tofu boxes just like any other engine, so S8-QA-02 must assert real glyphs, not merely that a PDF was produced. This decision does **not** resolve the EPPlus licensing debt, which remains open and blocking for production sign-off.

## ADR-0015: Cumulative-VO escalation is measured net-signed against the *baseline* contract value, resets on a formal contract amendment, and its threshold is a nullable per-policy config with no seeded default
Date: 2026-08-10 · Status: Accepted
Context: `docs/10.` R-04 required human confirmation before Sprint 10 on (a) whether the cumulative-VO escalation threshold is 10% and (b) whether the counter resets on contract amendment. `domain-expert` surfaced two sub-questions R-04 never asked that change the answer *more* than the threshold does. **Numerator:** net-signed vs gross-absolute vs additions-only produce **9.73% / 13.03% / 11.38% on identical data** — three different approval chains. **Denominator:** the shipped `ApprovalRoutingService.cs:66-70` divides by `request.ContractValue`, and the only such field is `Project.ContractValue`, defined as *"contract value incl. approved VOs"* — a **self-diluting denominator**, where every approval raises the divisor so the trigger recedes exactly as the contract drifts furthest from its original scope. That is a live defect in already-shipped Sprint 2 code, not a Sprint 10 design choice.
Decision: Threshold default **10.00%**, confirmed by the human 2026-08-10, and the counter **resets on a formal contract amendment**, re-baselining against the new contract value (also human-confirmed) — which makes `ContractAmendment` a real domain event, not a reporting convenience. Numerator is **net signed** (additions less deductions) and denominator is the **baseline contract value for the current cumulative window** (the original value, or the amended value after the most recent amendment). Neither of those two was guessed: `docs/10.`'s own R4 fixture states both a chain and a percentage (10.14%), and `domain-expert` verified that net-signed-over-baseline is the *only* combination reproducing both, so the plan's fixture uniquely determines the reading. FIDIC 4th ed. 52.3's net-against-Effective-Contract-Price test is the external reference point; the counter-argument is noted (Thai committee practice often tracks งานเพิ่ม separately, which would imply additions-only). `CumulativeVoEscalationPct` is **nullable**, `NULL` meaning "no escalation configured" and never "10", and `ApprovalPolicySeeder` seeds `NULL` — seeding a placeholder would make a guess indistinguishable from a decision in production data, which is precisely ADR-0008/M-04's failure mode.
Consequences: `Project` needs an `OriginalContractValue` (or amendment-baseline) field; the existing `ContractValue` cannot serve as the denominator and using it is a bug to fix, not a behaviour to preserve. A **real escalation bypass** follows from cumulative routing and must be closed in the same sprint: two VOs each under threshold at submission time can both be approved and take the contract across the threshold with no Executive signature anywhere. The fix is to re-check the ratio as a guard on the *final* approval and fail closed (409 + return for revision) — it must **not** append a step to the already-pinned chain, which is the H-01/N-01 bug class this codebase has already been burned by twice. Because the threshold is nullable, every consumer must treat "not configured" as "no escalation step", never as zero (which would escalate everything).

## ADR-0016: Quorum binds rejection exactly as it binds approval, and no actor may both approve and reject the same revision
Date: 2026-08-10 · Status: Accepted
Context: Sprint 9 security finding N-05, execution-verified: one actor approved (1 of 2) and then rejected, driving a `QuorumCount = 2` step to the terminal `Rejected` state alone. `Reject` had no `DuplicateChainApprover` equivalent and there is no withdraw/cancel recovery path (N-02). This was defensible under `approval-workflow.md` §6.1's literal text, which restricts only *approval* — but that silence was never a decision. Sprint 10 applies the same shape to Variation Orders, where refusing a VO can mean a contractor is denied payment for work already instructed, so the asymmetry hands one person a *larger* unilateral power than the dual control was configured to remove.
Decision: **Quorum binds rejection.** A `QuorumCount = q` step terminates only when *q* distinct role-holders reject, and no actor may cast both an approval and a rejection on the same revision — `DuplicateChainApprover` widens to `Action ∈ {Approve, Reject}` and is renamed `DuplicateChainVoter`. Human-confirmed 2026-08-10, explicitly including the instruction to fix Sprint 9's already-shipped Payment Certificate code rather than ship two inconsistent readings. `ReturnForRevision` stays deliberately **single-actor**, and that is what makes the rule deadlock-free: a `q=2` step holding one approval and one rejection satisfies neither quorum and both voters are blocked, so the lone dissenter returns it for revision — which is how a split committee actually behaves. Reasoning: the object of a quorum is the *decision*, not its direction; refusal is a positive act with contractual weight (FIDIC 3.5/3.7 makes it a formal determination appealable to the DAAB, and Thai งานเพิ่ม/งานลด are decided by a คณะกรรมการ acting by มติ, not by one member).
Consequences: Three behaviour changes to shipped, security-PASS-verified IPC code — `DuplicateChainVoter` on the reject handler, a reject-quorum parameter on `PaymentCertificate.Reject` mirroring `Approve`, and `LastVoteAt` stamped on non-terminal rejections. That last one is not optional: without it two concurrent first rejectors co-commit, which is exactly the N-03 failure mode already fixed for approvals. `QuorumCount = 1` behaviour is completely unchanged, so the overwhelmingly common configuration sees no difference — that must be asserted by test, not assumed. Because this alters already-audited Sprint 9 behaviour, `security-auditor` must re-verify it by execution under S10-SEC-01, inverting §9.5's original probe.

## ADR-0017: An ordinary BAC edit is blocked once an approved VO exists — it fails loudly rather than being silently ignored or silently rewriting history
Date: 2026-08-10 · Status: Accepted
Context: S10-SEC-02 finding M-02. Sprint 10 repointed every EVM read at $BAC(t) = OriginalBac + \Sigma(\text{approved VOs} \le t)$ so historical figures stop moving when a VO is approved (domain-rules.md §5.5(c)). But `Project.SetBac` still assigned the live `BAC` only, so a PM/QS editing the budget on the Project Info screen saw the value persist and display while the dashboard, EAC, VAC, TCPI and % complete all silently kept using the old figure — a behavioural regression into previously correct shipped code, with no warning anywhere. The obvious repair — have `SetBac` move `OriginalBac` by the same delta — was implemented and **immediately caught by an existing Domain test**, for a reason worth recording: applying the same treatment to its sibling `SetContractValue` reintroduces exactly the escalation bypass H-02 had just closed, because the cumulative-VO denominator *is* the baseline, so any ordinary edit that moves the baseline moves the trigger (ADR-0015's D-2). The two halves are not symmetric — `SetBac` affects EVM history, `SetContractValue` affects approval authority — and treating them alike is unsafe.
Decision: **Reject the edit when the project has at least one approved VO**, with an explicit error rather than a silent no-op; human-confirmed 2026-08-10, choosing this over "edit and rebase `OriginalBac`" and over splitting into two distinct operations. Rationale: it has no quiet failure mode in either direction — it neither ignores the user's input nor retroactively rewrites already-published EVM figures and diverges live queries from their closed `EvmPeriodSnapshot` rows. Before any VO is approved, `OriginalBac == BAC` and a plain edit remains correct and unrestricted, which covers the ordinary case of fixing the budget during setup. `SetContractValue` keeps its existing behaviour — it moves the live value only and never the escalation baseline, which the existing test `SetContractValue_Moves_ContractValue_But_Never_OriginalContractValue` already pins.
Consequences: A PM who genuinely needs to correct the base budget after VOs exist currently has no path, and the error message must say so plainly rather than implying the value is merely invalid. A deliberate **rebaseline** operation (re-cutting the baseline so prior VOs fold into it, with an audit record and its own authority) is the natural follow-up and should be specified before a customer hits this — treat the block as a safe stop, not a finished workflow. The §5.5(c) invariant `Project.BAC == BAC(now)` now holds unconditionally and should be asserted as such.

## ADR-0018: CM+ models per-project assignment; document authority is scoped to the projects a user is assigned to, not merely to their tenant
Date: 2026-08-10 · Status: Accepted
Context: Carried from Sprint 9 M-02 and re-raised as Sprint 10 M-04, both times recorded as an accepted limitation because closing it needs a product decision rather than a code fix. Today any authenticated tenant user holding the current chain step's role can act on **any** project's document in that tenant: a QS working on Project A can certify Project B's interim payment certificate, and approve Project B's Variation Order — which moves `Project.BAC` and `Project.ContractValue` directly. Nothing crosses a tenant boundary, so ADR-0002 is intact, but within a tenant there is no authorization boundary at all. No `ProjectMember`/`UserProject` concept exists anywhere in `backend/src`, so there was nothing to scope against; the flat document routes (`/api/v1/payment-certificates/{id}/…`, `/api/v1/variation-orders/{id}/…`) carry no `projectId` to cross-check either. Sprint 3's `GetImportJobQueryHandler` closes the same case, but only because its route happens to carry a `projectId`.
Decision: **Introduce per-project assignment and enforce it**, human-confirmed 2026-08-10 over the alternatives of continuing to accept the limitation or restricting only the money-moving endpoints. Enforcement belongs in the Application layer for every document command and query, with negative tests per project — not in the controllers, since the flat routes cannot express it and a filter would be bypassed by any future direct handler call. Multi-project construction firms are the norm rather than the exception, so "everyone in the tenant can act on everything" does not survive contact with a real customer; and the blast radius is a signed payment certificate or an approved variation, both of which are contractual acts.
Consequences: This is genuinely sprint-sized, not a patch — a new entity and migration, assignment management UI, a decision about whether `Admin`/`Executive` are tenant-wide by design (they almost certainly are), a backfill for existing data, and a change to every document handler plus its tests. Until it lands, Sprint 9 M-02 / Sprint 10 M-04 stay open and must keep appearing in each security review rather than being quietly dropped. The tenant filter (ADR-0002) stays exactly as it is — project scoping is an **additional**, narrower boundary layered on top, never a replacement, and it must not be implemented as a second global query filter that could mask a tenant-filter regression.

## ADR-0019: CPM results are versioned as an append-only `CpmRun` history, so criticality can be read as it stood on any past date
Date: 2026-08-10 · Status: Accepted
Context: Sprint 11's EOT evaluator must answer "was this activity critical?" as of the **stoppage date**, not as of today — `domain-expert` ruled this the contemporaneous reading, following SCL Delay & Disruption Protocol 2nd ed. Core Principle 4 (assess against the programme updated to immediately before the event; no "wait and see") and AACE RP 29R-03, which ranks Impacted As-Planned the least reliable method. The two alternatives are both defective: reading the *current* schedule lets a later unrelated re-sequencing retroactively extinguish a properly-recorded entitlement, and reading the *baseline* over-grants by crediting float the contractor had already spent. But `Activity.IsCritical`/`TotalFloat`/`FreeFloat` are three mutable fields overwritten on every CPM recalculation with no history, so the correct reading was **not implementable against the existing schema** — the evaluator additionally needs the network itself, since it re-runs `CpmEngine` on the governing relations rather than trusting stored flags.
Decision: Add an **append-only** `CpmRun` + `CpmRunActivity` + `CpmRunRelation` history; the governing run for a stoppage on date $d$ is $\arg\max\{r.\text{CalculatedAt} \le d\}$. Human-confirmed 2026-08-10, explicitly accepting that this exceeds S11-DB-01's original scope, over the offered alternative of shipping a degraded mode where every evaluation returns `Provisional`. The relation table is **not** optional — omitting it would force the evaluator to trust stored per-activity flags, which is the very thing the contemporaneous ruling exists to avoid. Use the established `IAppendOnly` marker and its interceptor rather than convention, per Sprint 9's M-01.
Consequences: Sprint 11 grows materially and its DB row must be re-scoped. Storage grows per recalculation, so a retention/pruning policy is needed before this runs at volume — but pruning a run that an evaluation cites would silently invalidate that evaluation, so retention must be defined against citation, not age alone. The history is independently valuable beyond EOT: baseline-vs-current comparison and delay-claim support both need it. Note this makes CPM recalculation a heavier write; the Gantt performance targets are read-path and unaffected, but the recalculation path should be measured rather than assumed.

## ADR-0020: Weather stoppage days are counted absolutely — every day genuinely lost — not as exceedance over a climatic norm
Date: 2026-08-10 · Status: Accepted
Context: `domain-expert`'s fixture W-13 showed the same weather data yielding **5 days on an absolute count and 2 days on a FIDIC-style exceedance count** — a 2.5× difference in schedule impact from a pure methodology choice, with no other input changing. FIDIC 8.5(c) frames entitlement around weather *exceeding* what was foreseeable for the season and location, which for Thai monsoon rain excludes a great deal of ordinary bad weather; an absolute count therefore states schedule impact faithfully but may overstate contractual entitlement, particularly under a Thai government contract.
Decision: Count **absolutely** — every day genuinely lost — human-confirmed 2026-08-10 over exceedance and over a per-project switch. This is coherent with the boundary `domain-expert` drew and this ADR affirms: the evaluator's output is `EotEligibleDays`, **a schedule fact, not an entitlement**. Entitlement additionally requires the weather to qualify as exceptional (FIDIC 8.4(c)/8.5(c)) or as เหตุสุดวิสัย under ป.พ.พ. ม.8 — which ordinary monsoon rain generally is not — plus notice within time (FIDIC 20.1/20.2.1's 28 days is a condition precedent; the Thai standard form allows 15 days from cessation). Absolute counting answers the schedule question the evaluator is actually competent to answer, and leaves the contractual filter to the humans who administer the contract.
Consequences: The prototype's gold tile "สิทธิ์ขยายสัญญา (EOT)" **must be relabelled** — presenting a schedule fact as an entitlement is precisely the conflation this decision separates, and the UI is where that error would reach a user. An evaluation writes its own record and an audit row and **nothing else**: no date moves, no duration changes, no `IsCritical` is written back, no ledger row, no CPM recalculation is triggered. That boundary is what makes the feature safe to ship before any contract-administration surface exists, and it should be asserted by test, not assumed. If exceedance is later wanted, it is an additional filter over the same absolute base — not a change to how days are counted — so this decision does not foreclose it.

## ADR-0021: The single-active filtered unique index is broken for NULL discriminator columns — `ApprovalPolicy` (tenant-wide default) can hold two active versions, silently
Date: 2026-08-11 · Status: **Accepted — implemented (Sprint 15 approval-policy hardening, same day). SQLite-proven; SQL Server behaviour inferred (standard ANSI semantics), not executed — Docker/SQL Server unavailable in this environment.**
Context: Sprint 14's Baseline work used the same "one active row per scope, enforced by a filtered unique index" pattern ADR-0008 established for `ApprovalPolicy`. QA and `backend-developer` probing it on SQLite (a real constraint-enforcing engine, since Docker/SQL Server is unavailable) uncovered two distinct concurrency defects and one architectural one:
  1. **Statement-batch ordering (Baseline only, fixed).** `ActivateBaselineCommandHandler` loaded `target` before `previousActive`, so EF's change-tracking order could emit the *activate* UPDATE before the *deactivate*, momentarily leaving two `IsActive=1` rows and tripping the index. Reproduced ~30–50% of trials. Fixed by splitting into two sequential `SaveChangesAsync` calls inside one transaction (`BaselineRepository.TryActivateAsync`), guarded `IsRelational()` because EF InMemory has no transactions. `ApprovalPolicy` was checked and is safe here — its handler tracks in the deactivate-first order by construction (Modified+Added vs Baseline's inverted Modified+Modified).
  2. **Unmapped `DbUpdateException` on a genuine two-request race (both, fixed).** The activate handlers caught only `DbUpdateConcurrencyException`; a real unique-index violation throws plain `DbUpdateException`, so two concurrent activations produced an unhandled 500 instead of a mapped 409. Fixed with a shared `UniqueIndexViolationClassifier` (SQL Server 2601/2627; SQLite extended code 2067, matched without a production `Microsoft.Data.Sqlite` dependency), returning a `ConcurrencyConflict` result mapped to 409. Anything that is not a unique-index violation still propagates, per `ProjectRepository`'s existing discipline against catching `DbUpdateException` broadly.
Decision (the defect this ADR exists to record): **The filtered unique index cannot enforce single-active when its discriminator is NULL.** `ApprovalPolicy.ProjectId` is nullable, and *every policy the shipped code can create today has `ProjectId = null`* (tenant-wide default; the project-scoped override is schema-present but unexposed until Sprint 15). Standard ANSI/SQL-Server semantics treat NULL ≠ NULL for uniqueness, so `(TenantId, ProjectId, DocumentType) WHERE IsActive = 1` **never fires** when two competing active rows both have `ProjectId = null`. Two concurrent `UpdateApprovalPolicy` requests therefore both succeed, leaving **two simultaneously-active policy versions permanently** — silent data corruption of the approval engine's single-active invariant, which no exception handling can catch because the database raises nothing. Proven in `ApprovalPolicyActivationConcurrencySqliteTests`. This is not a security *bypass* in the H-01 sense — in-flight documents pin a specific `ApprovalPolicyId` and are unaffected — but a *new* submission then resolves "the currently active policy" nondeterministically between the two, so which delegation-of-authority routes a fresh payment certificate or VO becomes a race outcome.
Consequences: The fix is an **index redesign, not code** — e.g. two filtered indexes, one keyed `(TenantId, DocumentType) WHERE IsActive = 1 AND ProjectId IS NULL` for the tenant-default case and the existing one `AND ProjectId IS NOT NULL` for overrides; or a computed non-null discriminator. Either supersedes ADR-0008's index definition and needs a migration, so it is a `database-engineer`/`system-architect` decision with human sign-off, deliberately **not** taken unilaterally. Until then the corruption requires two tenant-admins editing the same tenant's policy in the same short window — rare, but the blast radius is the security-critical routing engine, so it should be scheduled into Sprint 15 (which already owns approval-policy hardening) rather than left open indefinitely. **Baseline is not exposed to the NULL variant** — its index discriminator `ProjectId` is non-nullable — so Baseline's single-active guarantee is sound once (1) and (2) are fixed, which they are. The reusable lesson: a filtered unique index on a nullable column silently does nothing for the NULL group; never rely on one to enforce single-active over a column that can be NULL.

**Implementation update (2026-08-11, `database-engineer`, same-day close-out).** Took the ADR's own recommended fix: `ApprovalPolicyConfiguration` now ships exactly the two filtered indexes named above — `(TenantId, DocumentType) WHERE IsActive = 1 AND ProjectId IS NULL` (closes the live defect) and `(TenantId, ProjectId, DocumentType) WHERE IsActive = 1 AND ProjectId IS NOT NULL` (the original ADR-0008 guarantee, narrowed so the two are provably disjoint). Verified against the real query shapes, not just this ADR's sentence: `ApprovalPolicyReader.GetActiveTenantDefaultPolicyAsync`/`ApprovalPolicyRepository.FindActiveTenantDefaultAsync` filter exactly `(TenantId, DocumentType, ProjectId IS NULL, IsActive)` — a full seek on the first index; `GetCandidatePoliciesAsync` filters `(TenantId, DocumentType, IsActive)` with no `ProjectId` predicate at all, because a tenant default and a project override for the same document type must both be legitimate simultaneous candidates — confirmed by a new test proving they coexist as active, which is the whole reason for splitting into two indexes rather than one computed discriminator. Migration `20260811134718_Sprint15_ApprovalPolicy_SplitSingleActiveIndex` is additive/reversible (drops the one old index, creates two new ones; `Down` restores the original single index exactly). Ordering check (this ADR's own §1 finding, re-verified for the new shape): `UpdateApprovalPolicyCommandHandler`'s single `SaveChangesAsync` batch (one `Modified` deactivate + one `Added` insert) has no Baseline-style statement-ordering hazard — confirmed by 30 sequential trials against the real production configuration, both for the tenant-default group (the one that changed) and the project-scoped group (unchanged, re-verified for completeness). **Red/green proof, both executed on SQLite:** `ApprovalPolicyActivationConcurrencySqliteTests.Tenant_Wide_Default_Policy_ProjectId_Is_Null_The_Split_Index_Now_Rejects_A_Second_Simultaneously_Active_Version` is the green half (fails if the fix is reverted — confirmed by mutation: reverting `ApprovalPolicyConfiguration` to the old single index makes exactly this test fail with "No exception was thrown", nothing else); `Mutation_Proof_The_Old_Single_Index_Would_Not_Have_Caught_This_The_New_Split_Index_Does` is the red half, executed permanently in the same file against a locally-replicated pre-fix schema, not merely narrated. **Deployment honesty:** applying `CREATE UNIQUE INDEX ... WHERE IsActive = 1 AND ProjectId IS NULL` to a database that *already* holds this corruption (two active null-`ProjectId` rows for the same tenant/document type) fails outright — SQL Server validates uniqueness over existing rows at CREATE INDEX time, same as SQLite, both standard ANSI behaviour. That failure is correct (the database refusing to silently pick a winner), but it means the migration cannot be fire-and-forget in the deployment runbook: a pre-flight `GROUP BY (TenantId, DocumentType) HAVING COUNT(*) > 1` dedup check (`artifacts/migrations/20260811_sprint15_approvalpolicy_split_singleactive_index.PREFLIGHT.sql`, same shape as the Sprint 10 N-04 IPC-index check) must return zero rows first, with a documented one-row-at-a-time human remediation path if it doesn't. **Still unproven:** real SQL Server execution — everything above is SQLite plus ANSI-semantics inference, consistent with every other ADR-0021 claim about SQL Server; Docker/SQL Server remains unavailable in this environment.
